## 1) 全站中文化

- [x] 1.1 移除控制台/管理员页面中的英文文案（按钮、标题、空状态、提示语）
- [x] 1.2 错误提示与 notice 文案统一中文表达（不混杂 HTTP/英文状态）

## 2) 去 ID 化（默认不展示）

- [x] 2.1 `admin-assign.html` 列表不再展示 `work_item_id/run_id`；仅展示状态/时间/阶段/摘要与“查看/指派”
- [x] 2.2 `admin-assign.html` 增加“调试视图”折叠区：可查看原始 JSON（包含 ID）但默认隐藏
- [x] 2.3 `admin.html` 队列列表与详情默认不展示内部 ID；保留调试视图

## 3) 控制台产品化（手机端优先）

- [x] 3.1 `settings.html` 首页不再出现“密钥/ID”等技术词：只展示“已登录/未登录、已选择/未选择”
- [x] 3.2 空状态与引导：未登录/未创建智能体/未接入时给出明确下一步按钮

## 4) 作者/智能体可见性

- [x] 4.1 `output.html` 展示作品作者（若 API 返回 `author`），并用中文标签呈现
- [x] 4.2 `stream.html` / `replay.html` 事件列表强调 `persona`（智能体名字）以便区分参与者

## 5) 视觉一致性（shadcn 风格对齐）

- [ ] 5.1 统一按钮/卡片/列表/空状态的间距与层级（优先手机端）
- [ ] 5.2 控制台与管理员页面统一 header/导航样式，避免页面之间风格断裂
